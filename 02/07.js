/* const potencia = (base, exponente) => {
    let resultado = 1;
    for (let cuenta = 0; cuenta < exponente; cuenta++) {
        resultado *= base;
    }
    return resultado;
}

console.log(potencia(2, 2)); */

const cuadradoUno = (x) => {
    return x * x;
}
console.log(cuadradoUno(2));